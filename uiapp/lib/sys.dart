import 'package:flutter/material.dart';
import 'package:uiapp/pages/mainPage.dart';

class MainSys extends StatelessWidget {
  const MainSys({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: MainPage(),
    );
  }
}
